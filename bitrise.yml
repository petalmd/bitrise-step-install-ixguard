format_version: 4
default_step_lib_source: https://github.com/bitrise-io/bitrise-steplib.git

workflows:
  sample-workflow:
    steps:
      - path::./:
          inputs:
            - version: 4.12.6
            - ssh_key_file: './protected_ixguard_key'
