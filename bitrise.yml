format_version: 4
default_step_lib_source: https://github.com/bitrise-io/bitrise-steplib.git

workflows:
  sample-workflow:
    steps:
      - path::./:
          inputs:
            - version: 4.15.0
            - ssh_key_file: ./guardsquare_key
            - ssh_key_passphrase: $GUARDSQUARE_SSH_KEY_PASSPHRASE
